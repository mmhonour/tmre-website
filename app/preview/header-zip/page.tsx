import HeaderZipPreviewClient from "./HeaderZipPreviewClient";

export const metadata = {
  title: "Preview — Header ZIP, no halo — TMRE",
  robots: { index: false, follow: false },
};

export default function HeaderZipPreviewPage() {
  return <HeaderZipPreviewClient />;
}
