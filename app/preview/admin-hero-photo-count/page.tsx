import AdminHeroPhotoCountPreviewClient from "./AdminHeroPhotoCountPreviewClient";

export const metadata = {
  title: "Preview — Admin listings + R2 photo index — TMRE",
  robots: { index: false, follow: false },
};

export default function AdminHeroPhotoCountPreviewPage() {
  return <AdminHeroPhotoCountPreviewClient />;
}
