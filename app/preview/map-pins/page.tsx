import MapPinsPreviewClient from "./MapPinsPreviewClient";

export const metadata = {
  title: "Preview — Map pins — TMRE",
  robots: { index: false, follow: false },
};

export default function MapPinsPreviewPage() {
  return <MapPinsPreviewClient />;
}
