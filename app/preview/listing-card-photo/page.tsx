import { ListingCardPhotoPreview } from "./ListingCardPhotoPreview";

export const metadata = {
  title: "Preview — Mid-size card photos — TMRE",
  robots: { index: false, follow: false },
};

export default function ListingCardPhotoPreviewPage() {
  return <ListingCardPhotoPreview />;
}
