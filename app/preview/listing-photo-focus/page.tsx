import ListingPhotoFocusPreview from "./ListingPhotoFocusPreview";

export const metadata = {
  title: "Preview — Listing photo full screen — TMRE",
  robots: { index: false, follow: false },
};

export default function ListingPhotoFocusPreviewPage() {
  return <ListingPhotoFocusPreview />;
}
