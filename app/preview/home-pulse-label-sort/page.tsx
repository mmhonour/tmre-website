import HomePulseLabelSortPreviewClient from "./HomePulseLabelSortPreviewClient";

export const metadata = {
  title: "Preview — Home pulse label sort — TMRE",
  robots: { index: false, follow: false },
};

export default function HomePulseLabelSortPreviewPage() {
  return <HomePulseLabelSortPreviewClient />;
}
