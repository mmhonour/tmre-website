import AdminSyncSortPreviewClient from "./AdminSyncSortPreviewClient";

export const metadata = {
  title: "Preview — Admin sync column sort — TMRE",
  robots: { index: false, follow: false },
};

export default function AdminSyncSortPreviewPage() {
  return <AdminSyncSortPreviewClient />;
}
