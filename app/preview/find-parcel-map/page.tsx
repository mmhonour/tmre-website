import FindParcelMapPreview from "./FindParcelMapPreview";

export const metadata = {
  title: "Preview — VGSI neighborhood map — TMRE",
  robots: { index: false, follow: false },
};

export default function FindParcelMapPreviewPage() {
  return <FindParcelMapPreview />;
}
