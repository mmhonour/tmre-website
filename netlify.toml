[build]
  # Sync runs on the Netlify schedule (sync-listings function), not at build time —
  # build-time sync needs RETS env vars and breaks when .env.local is absent.
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  LISTINGS_DB_PATH = "/tmp/listings.db"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["rets-client", "node-expat", "better-sqlite3", "bindings"]
  included_files = [
    "node_modules/rets-client/**",
    "node_modules/node-expat/**",
    "node_modules/better-sqlite3/**",
    "node_modules/bindings/**",
    "data/listings.bundle.db",
  ]
