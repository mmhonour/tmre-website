/** Panel frames — dashed outlines for layout review */
export const homeFrameClass =
  "rounded-2xl border-2 border-dashed border-gold/70 bg-white/[0.04] p-6 min-w-0";

/** Outer layout regions (grids, columns, section shells) */
export const homeRegionOutlineClass =
  "rounded-2xl border-2 border-dashed border-coral/55 p-4 min-w-0";

/** Inner sections inside a panel */
export const homeSectionOutlineClass =
  "rounded-xl border-2 border-dashed border-white/45 p-4 min-w-0";
