# Copy to railway-mls-sync.env (gitignored) or paste into Railway → Variables → Raw Editor.
# Values must match Netlify / Neon production.

DATABASE_URL=
RETS_SERVER_URL=
RETS_USERNAME=
RETS_PASSWORD=
SYNC_CRON_SECRET=
MLS_SYNC_INTERVAL_MS=1800000
